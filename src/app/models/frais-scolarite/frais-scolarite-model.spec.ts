import { FraisScolariteModel } from './frais-scolarite-model';

describe('FraisScolariteModel', () => {
  it('should create an instance', () => {
    expect(new FraisScolariteModel()).toBeTruthy();
  });
});
